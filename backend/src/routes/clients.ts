import {Router} from 'express'; import {Role} from '@prisma/client'; import {z} from 'zod'; import {prisma} from '../lib/prisma.js'; import {requireAuth,requireRoles} from '../middleware/auth.js'; import {validate} from '../middleware/validate.js'; import {asyncHandler} from '../utils/http.js';
const r=Router(); r.use(requireAuth); const body=z.object({name:z.string().min(2),email:z.string().email().optional(),company:z.string().optional()}); r.get('/',asyncHandler(async(_req,res)=>res.json({success:true,data:await prisma.client.findMany({orderBy:{name:'asc'}})}))); r.post('/',requireRoles(Role.ADMIN),validate(body),asyncHandler(async(req,res)=>res.status(201).json({success:true,data:await prisma.client.create({data:req.body})}))); export default r;
